 FreeAlign 完整训练流程详解

   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                              train.py 入口                                       │
   │                        D:\FreeAlign\opencood\tools\train.py                      │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  第47行: opt = train_parser()                                                    │
   │  └── 解析命令行参数: --hypes_yaml, --model_dir, --fusion_method                  │
   │                                                                                  │
   │  第49行: hypes = yaml_utils.load_yaml(opt.hypes_yaml, opt)                      │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                         1. 配置加载 (yaml_utils.py)                              │
   │                    D:\FreeAlign\opencood\hypes_yaml\yaml_utils.py                │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  load_yaml() 函数:                                                               │
   │  ├── 打开 YAML 文件流                                                            │
   │  ├── yaml.load() 解析配置字典                                                    │
   │  └── 第28行: param = eval(param["yaml_parser"])(param)                          │
   │      └── 调用 load_point_pillar_params() 解析锚框参数                             │
   │                                                                                  │
   │  返回配置字典 hypes:                                                              │
   │  ├── hypes['model']          → 模型架构配置                                       │
   │  ├── hypes['loss']           → 损失函数配置                                       │
   │  ├── hypes['optimizer']      → 优化器配置                                         │
   │  ├── hypes['fusion']         → 融合方法配置                                       │
   │  └── hypes['box_align']      → FreeAlign 配置 ★                                   │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                         2. 数据集构建 (build_dataset)                            │
   │                   D:\FreeAlign\opencood\data_utils\datasets\__init__.py          │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  build_dataset(hypes, visualize=False, train=True)                              │
   │  │                                                                               │
   │  ├── 第9行: fusion_name = 'intermediate' (中间融合)                              │
   │  ├── 第10行: dataset_name = 'dairv2x' (DAIR-V2X数据集)                           │
   │  │                                                                               │
   │  ├── 第14行: fusion_dataset_func = getIntermediateFusionDataset                 │
   │  ├── 第15行: base_dataset_cls = DAIRV2XBaseDataset                              │
   │  │                                                                               │
   │  └── 第18-22行: 返回数据集实例                                                    │
   │       dataset = IntermediateFusionDataset(DAIRV2XBaseDataset)(hypes)             │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    3. 数据集初始化 (IntermediateFusionDataset)                   │
   │           D:\FreeAlign\opencood\data_utils\datasets\intermediate_fusion_dataset.py│
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  __init__() 构造函数:                                                            │
   │  │                                                                               │
   │  ├── 第26-44行: 初始化父类 (DAIRV2VBaseDataset)                                  │
   │  │   ├── 设置数据路径                                                            │
   │  │   ├── 加载 JSON 索引文件                                                      │
   │  │   └── 初始化预处理器/后处理器                                                  │
   │  │                                                                               │
   │  └── 第57-71行: FreeAlign 参数初始化 ★                                           │
   │      ├── self.box_align = True                                                   │
   │      ├── self.stage1_result_path = "stage1_boxes.json"                          │
   │      ├── self.no_pose = True (不使用外部位姿)                                     │
   │      ├── self.min_anchor = 4 (最小锚点数)                                        │
   │      ├── self.anchor_error = 0.3                                                 │
   │      └── self.box_error = 0.5                                                    │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                         4. 模型创建 (create_model)                               │
   │                     D:\FreeAlign\opencood\tools\train_utils.py                   │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  create_model(hypes) 函数:                                                       │
   │  │                                                                               │
   │  ├── 第76行: backbone_name = 'point_pillar_baseline_multiscale'                 │
   │  │                                                                               │
   │  ├── 第79-86行: 动态导入模型模块                                                  │
   │  │   model_filename = "opencood.models.point_pillar_baseline_multiscale"        │
   │  │   model_lib = importlib.import_module(model_filename)                        │
   │  │                                                                               │
   │  └── 第94行: 实例化模型                                                          │
   │      model = PointPillarBaselineMultiscale(backbone_config)                     │
   │      │                                                                           │
   │      模型结构:                                                                    │
   │      ├── pillar_vfe: 体素特征编码器                                              │
   │      ├── scatter: 特征散射到伪图像                                                │
   │      ├── backbone: ResNet BEV主干网络                                            │
   │      ├── fusion_net: Max融合模块 (多尺度)                                        │
   │      ├── cls_head: 分类头                                                        │
   │      ├── reg_head: 回归头                                                        │
   │      └── dir_head: 方向分类头                                                    │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                         5. 损失函数创建 (create_loss)                            │
   │                     D:\FreeAlign\opencood\tools\train_utils.py                   │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  create_loss(hypes) 函数:                                                        │
   │  │                                                                               │
   │  ├── 第108行: loss_func_name = 'point_pillar_loss'                              │
   │  │                                                                               │
   │  └── 第123行: criterion = PointPillarLoss(config)                               │
   │      │                                                                           │
   │      损失组成:                                                                    │
   │      ├── cls_loss: SigmoidFocalLoss (分类损失)                                   │
   │      ├── reg_loss: WeightedSmoothL1Loss (回归损失)                               │
   │      └── dir_loss: SoftmaxCrossEntropy (方向损失)                                │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                         6. 训练主循环 (Training Loop)                            │
   │                        D:\FreeAlign\opencood\tools\train.py                      │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  for epoch in range(init_epoch, epoches):                                        │
   │      │                                                                           │
   │      └── for i, batch_data in enumerate(train_loader):                          │
   │          │                                                                       │
   │          ├── 第102行: batch_data = train_utils.to_device(batch_data, device)   │
   │          │                                                                       │
   │          ├── 第105行: output_dict = model(batch_data['ego'])  ★ 前向传播        │
   │          │                                                                       │
   │          ├── 第107行: loss = criterion(output_dict, label_dict)  ★ 损失计算     │
   │          │                                                                       │
   │          ├── 第116行: loss.backward()  ★ 反向传播                                │
   │          │                                                                       │
   │          └── 第117行: optimizer.step()  ★ 参数更新                               │
   └─────────────────────────────────────────────────────────────────────────────────┘

  ---

  详细展开：数据加载流程 (FreeAlign 核心所在)

   ... first 86 line$s$ hidden ... (ctrl+r to expand)
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  对每个 CAV 执行:                                                                 │
   │  │                                                                               │
   │  ├── 第88-95行: 计算变换矩阵                                                      │
   │  │   transformation_matrix = x1_to_x2(cav_pose, ego_pose)                       │
   │  │   └── 计算 CAV 到 Ego 的相对变换                                               │
   │  │                                                                               │
   │  ├── 第102-110行: 点云预处理                                                      │
   │  │   ├── shuffle_points(): 打乱点云顺序                                          │
   │  │   ├── mask_ego_points(): 移除打到自身的点                                      │
   │  │   └── pre_processor.preprocess(): 体素化                                       │
   │  │       ├── 体素化: 点云 → 体素网格                                              │
   │  │       └── 输出: voxel_features, voxel_coords, voxel_num_points               │
   │  │                                                                               │
   │  └── 第117-124行: 生成标签                                                        │
   │      ├── generate_object_center(): 生成真值框中心                                 │
   │      └── post_processor.generate_label(): 生成训练标签                            │
   │          ├── pos_equal_one: 正样本掩码                                            │
   │          ├── neg_equal_one: 负样本掩码                                            │
   │          └── targets: 回归目标                                                    │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    10. 批处理整理 (collate_batch_train)                          │
   │           D:\FreeAlign\opencood\data_utils\datasets\intermediate_fusion_dataset.py│
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  将多个样本整理成一个 batch:                                                       │
   │  │                                                                               │
   │  ├── 第501-530行: 收集各样本数据                                                  │
   │  │   ├── object_bbx_center: 真值框中心                                           │
   │  │   ├── processed_lidar: 体素化点云                                              │
   │  │   ├── record_len: 每个 batch 的 CAV 数量                                       │
   │  │   ├── label_dict: 标签字典                                                     │
   │  │   └── pairwise_t_matrix: 成对变换矩阵                                          │
   │  │                                                                               │
   │  ├── 第545-555行: 合并预处理特征                                                  │
   │  │   merged_feature_dict = merge_features_to_dict(processed_lidar_list)         │
   │  │   processed_lidar_torch_dict = pre_processor.collate_batch(merged_feature)   │
   │  │                                                                               │
   │  └── 第572-580行: 返回 batch 数据字典                                             │
   │      output_dict['ego'] = {                                                      │
   │          'processed_lidar': 体素数据,                                             │
   │          'record_len': [N_cav_per_sample, ...],                                  │
   │          'pairwise_t_matrix': 变换矩阵,                                           │
   │          'label_dict': 标签                                                       │
   │      }                                                                           │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    11. 模型前向传播 (model.forward)                              │
   │        D:\FreeAlign\opencood\models\point_pillar_baseline_multiscale.py          │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  forward(data_dict) 执行流程:                                                     │
   │  │                                                                               │
   │  ├── 第103-106行: 体素特征编码                                                    │
   │  │   batch_dict = self.pillar_vfe(batch_dict)                                   │
   │  │   └── 输出: [N_voxel, C] 特征向量                                              │
   │  │                                                                               │
   │  ├── 第108-109行: 特征散射                                                        │
   │  │   batch_dict = self.scatter(batch_dict)                                      │
   │  │   └── 输出: [N, C, H, W] 伪图像 (BEV特征图)                                    │
   │  │                                                                               │
   │  ├── 第111-113行: 计算成对变换矩阵                                                │
   │  │   t_matrix = normalize_pairwise_tfm(pairwise_t_matrix, H, W, voxel_size)    │
   │  │   └── 将位姿变换转换为特征图上的仿射变换                                        │
   │  │                                                                               │
   │  ├── 第119-123行: 多尺度特征提取                                                  │
   │  │   feature_list = self.backbone.get_multiscale_feature(spatial_features)      │
   │  │   └── 输出: 3个尺度的特征 [C1,H1,W1], [C2,H2,W2], [C3,H3,W3]                  │
   │  │                                                                               │
   │  ├── 第126-128行: 多尺度融合 ★                                                    │
   │  │   for fuse_module in self.fusion_net:                                         │
   │  │       fused_feature = fuse_module(feature, record_len, t_matrix)             │
   │  │   └── 使用校正后的 t_matrix 进行特征对齐和融合                                  │
   │  │                                                                               │
   │  ├── 第130-131行: 特征解码                                                        │
   │  │   fused_feature = self.backbone.decode_multiscale_feature(fused_list)        │
   │  │   └── 上采样并拼接多尺度特征                                                   │
   │  │                                                                               │
   │  └── 第136-141行: 预测头                                                          │
   │      ├── psm = self.cls_head(fused_feature)  → 分类预测                          │
   │      ├── rm = self.reg_head(fused_feature)   → 回归预测                          │
   │      └── dm = self.dir_head(fused_feature)   → 方向预测                          │
   │                                                                                  │
   │  返回: output_dict = {'cls_preds': psm, 'reg_preds': rm, 'dir_preds': dm}       │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    12. 损失计算 (criterion.forward)                              │
   │                D:\FreeAlign\opencood\loss\point_pillar_loss.py                   │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  forward(output_dict, target_dict) 执行流程:                                      │
   │  │                                                                               │
   │  ├── 第34-39行: 准备正负样本掩码                                                  │
   │  │   positives = pos_equal_one > 0  # 正样本                                      │
   │  │   negatives = neg_equal_one > 0  # 负样本                                      │
   │  │   pos_normalizer = positives.sum()  # 正样本数量                               │
   │  │                                                                               │
   │  ├── 第49-54行: 分类损失                                                  │
   │  │   cls_loss = sigmoid_focal_loss(cls_preds, cls_labels, weights)              │
   │  │   ├── alpha = 0.25, gamma = 2.0                                               │
   │  │   └── 解决正负样本不平衡问题                                                   │
   │  │                                                                               │
   │  ├── 第57-61行: 回归损失 (Smooth L1)                                             │
   │  │   reg_loss = weighted_smooth_l1_loss(reg_preds, reg_targets, weights)        │
   │  │   ├── sigma = 3.0                                                             │
   │  │   └── 只对正样本计算回归损失                                                   │
   │  │                                                                               │
   │  ├── 第65-70行: 方向损失                                              │
   │  │   dir_targets = get_direction_target(reg_targets)                            │
   │  │   dir_loss = softmax_cross_entropy_with_logits(dir_logits, dir_targets)      │
   │  │   └── 解决方向歧义问题 (180度混淆)                                             │
   │  │                                                                               │
   │  └── 第79-83行: 总损失                                                            │
   │      total_loss = cls_loss * 2.0 + reg_loss * 2.0 + dir_loss * 0.2              │
   │      └── 返回标量损失值                                                           │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    13. 反向传播 & 参数更新                                        │
   │                        D:\FreeAlign\opencood\tools\train.py                      │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  第116-117行:                                                                     │
   │  │                                                                               │
   │  ├── final_loss.backward()                                                       │
   │  │   ├── 计算梯度 ∂L/∂w                                                          │
   │  │   ├── 链式法则反向传播                                                         │
   │  │   │   ├── dir_head → dir_loss                                                 │
   │  │   │   ├── reg_head → reg_loss                                                 │
   │  │   │   ├── cls_head → cls_loss                                                 │
   │  │   │   ├── backbone → fused_feature                                            │
   │  │   │   ├── fusion_net → feature_list                                           │
   │  │   │   ├── scatter → spatial_features                                          │
   │  │   │   └── pillar_vfe → voxel_features                                         │
   │  │   └── 梯度累积到各层参数                                                       │
   │  │                                                                               │
   │  └── optimizer.step()                                                            │
   │      ├── Adam 更新规则: w = w - lr * grad / (sqrt(v) + eps)                     │
   │      ├── lr = 0.002                                                              │
   │      └── weight_decay = 1e-4 (L2正则化)                                          │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    14. 验证 & 模型保存                                           │
   │                        D:\FreeAlign\opencood\tools\train.py                      │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  每个 eval_freq epoch 执行验证:                                                   │
   │  │                                                                               │
   │  ├── 第89-108行: 验证循环                                                        │
   │  │   ├── model.eval()                                                            │
   │  │   ├── with torch.no_grad()                                                    │
   │  │   └── 计算验证集平均损失                                                       │
   │  │                                                                               │
   │  ├── 第110-120行: 保存最佳模型                                                    │
   │  │   if valid_loss < lowest_val_loss:                                            │
   │  │       torch.save(model.state_dict(), 'net_epoch_bestval_at%d.pth')           │
   │  │                                                                               │
   │  └── 第125-127行: 定期保存                                                        │
   │      if epoch % save_freq == 0:                                                  │
   │          torch.save(model.state_dict(), 'net_epoch%d.pth')                      │
   └─────────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
   ┌─────────────────────────────────────────────────────────────────────────────────┐
   │                    15. 学习率调度                                                │
   │                        D:\FreeAlign\opencood\tools\train.py                      │
   ├─────────────────────────────────────────────────────────────────────────────────┤
   │  第128行: scheduler.step(epoch)                                                  │
   │  │                                                                               │
   │  └── MultiStepLR 调度器:                                                          │
   │      ├── 初始 lr = 0.002                                                         │
   │      ├── epoch 10: lr = 0.002 * 0.1 = 0.0002                                    │
   │      └── epoch 15: lr = 0.0002 * 0.1 = 0.00002                                  │
   └─────────────────────────────────────────────────────────────────────────────────┘

  ---

  FreeAlign 在训练流程中的关键位置总结

✦
  ┌──────────┬───────────────────────────────────┬─────────┬───────────────────────────────────┐
  │ 阶段     │ 文件                              │ 行号    │ 功能                              │
  ├──────────┼───────────────────────────────────┼─────────┼───────────────────────────────────┤
  │ 配置读取 │ intermediate_fusion_dataset.py      │ 57-71   │ 读取 box_align 参数               │
  │ 结果加载 │ intermediate_fusion_dataset.py      │ 330-340 │ 加载 Stage1 预计算检测框          │
  │ 对齐执行 │ intermediate_fusion_dataset.py      │ 359-370 │ 调用 box_alignment_relative_sample_np │
  │ 距离计算 │ box_align_v2.py                     │ 178-194 │ 计算检测框对 L2 距离              │
  │ 图优化   │ box_align_v2.py                     │ 240-280 │ g2o 位姿图优化                    │
  │ 位姿更新 │ intermediate_fusion_dataset.py      │ 372-373 │ 更新 CAV 位姿                     │
  │ 特征融合 │ point_pillar_baseline_multiscale.py │ 126-128 │ 使用校正后位姿融合特征            │
  └──────────┴───────────────────────────────────┴─────────┴───────────────────────────────────┘